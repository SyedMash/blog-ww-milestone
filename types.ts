export type Posts = {
    name: string;
    slug: string;
    description: string;
    image: any;
}

export type Post = {
    name: string;
    description: string;
    image: any;
    content: any;
}